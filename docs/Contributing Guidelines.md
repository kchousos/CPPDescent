# Contributing Guidelines

## Adding new features

Every new feature should first be written in a separate branch and then be
submitted through a Pull Request (PR). The PR will be assigned to both members
of the team and will need to be approved by both before it is merged.
